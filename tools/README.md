# Tools

Repository-level scripts and automation helpers can live here when they become necessary.

The foundation phase now reserves:

- `tools/db-migrations` for auxiliary migration scripts, backfills, and operator documentation
- `tools/seed` for auxiliary seed helpers and operator documentation
